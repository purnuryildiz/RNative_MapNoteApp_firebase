import {StyleSheet} from 'react-native';
import Colors from '../theme/colors';

// define your styles
const uiElementStyles = StyleSheet.create({
  input: {
    marginVertical: 10,
  },
  button: {
    marginVertical: 10,
  },
});

//make this component available to the app
export default uiElementStyles;
